# Скопируйте в .env и заполните значения
# Для SQLite (по умолчанию) DATABASE_URL не нужен
JWT_SECRET="your-super-secret-key-change-in-production"
JWT_EXPIRES_IN="7d"
PORT=3001

# Администратор: логин и пароль создаются при первом запуске npx prisma db seed.
# Задайте их здесь для продакшена (пароль не короче 10 символов).
# Если ADMIN_PASSWORD не задан — при seed будет сгенерирован случайный пароль (выведен в консоль один раз).
ADMIN_LOGIN=admin
ADMIN_PASSWORD=придумайте_надёжный_пароль_не_короче_10_символов

# Telegram-бот (опционально): привязка аккаунта и уведомления о пожарных сигналах
# Создайте бота через @BotFather, вставьте сюда токен и перезапустите backend
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
